services:
  backend:
    build: .
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-8080}:8080"
    env_file:
      - .env
    volumes:
      - ./storage:/app/storage
      - ./data:/app/data
