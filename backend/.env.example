OPENROUTER_API_KEY=
OPENROUTER_LLM_MODEL=google/gemini-2.5-flash

# Porta esposta sull'host (default 8080). Su Raspberry con 8080 occupata usa es. 8082.
BACKEND_PORT=8080

# Opzionale — solo se avvii cloudflared con: docker compose --profile tunnel up -d
CLOUDFLARE_TUNNEL_TOKEN=
